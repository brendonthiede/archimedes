apiVersion: v1
kind: Service
metadata:
  name: {{ include "archimedes-property-operator.fullname" . }}
  labels:
    {{- include "archimedes-property-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "archimedes-property-operator.selectorLabels" . | nindent 4 }}
