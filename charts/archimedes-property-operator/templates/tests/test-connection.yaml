apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "archimedes-property-operator.fullname" . }}-test-connection"
  labels:
    {{- include "archimedes-property-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "archimedes-property-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
